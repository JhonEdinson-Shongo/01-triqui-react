# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: build proyect

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js 14.21.3
      uses: actions/setup-node@v3
      with:
        node-version: 14.21.3
    - run: npm install
    - run: npm run build
    - run: |
          git config user.name Jhon Edinson Castañeda Oviedo
          git config user.email 48686871+JhonEdinson-Shongo@users.noreply.github.com
          git checkout -b newdist
          git add --force dist
          git commit -m "new dist"
          git push -u origin newdist
          git push origin `git subtree split --prefix dist newdist`:gh-pages --force
          git push origin --delete newdist
